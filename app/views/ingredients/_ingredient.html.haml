%a{ href: edit_ingredient_path(ingredient) }
  .ingredient{ id: dom_id(ingredient) }
    %h3= ingredient.name

    %div
      %small
        = 'Quantity'
        = ':'
      = "#{ingredient.quantity}"
